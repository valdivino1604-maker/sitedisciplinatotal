const revealItems=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('on')}})},{threshold:.14});revealItems.forEach(item=>observer.observe(item));const chatLog=document.querySelector('[data-chat-log]');const chatInput=document.querySelector('[data-chat-input]');const chatBtn=document.querySelector('[data-chat-button]');function botAnswer(question){const q=(question||'').toLowerCase();if(q.includes('procrast'))return 'Sim. O método foi feito justamente para quem procrastina, começando com passos simples para criar ação diária.';if(q.includes('tempo')||q.includes('dia'))return 'A ideia é aplicar pequenos blocos todos os dias. O mais importante é constância, não perfeição.';if(q.includes('serve')||q.includes('mim'))return 'Serve se você quer organizar rotina, ter mais foco e parar de depender só de motivação.';if(q.includes('garantia')||q.includes('compra'))return 'A compra é feita pela Hotmart, com segurança e regras de acesso/garantia da plataforma.';return 'O livro ajuda você a transformar intenção em rotina, usando clareza, ambiente, repetição e identidade disciplinada.'}function addBubble(text,type){if(!chatLog)return;const div=document.createElement('div');div.className='bubble '+type;div.textContent=text;chatLog.appendChild(div);chatLog.scrollTop=chatLog.scrollHeight}function sendQuestion(){if(!chatInput)return;const text=chatInput.value.trim();if(!text)return;addBubble(text,'user');chatInput.value='';setTimeout(()=>addBubble(botAnswer(text),'bot'),350)}if(chatBtn)chatBtn.addEventListener('click',sendQuestion);if(chatInput)chatInput.addEventListener('keydown',e=>{if(e.key==='Enter')sendQuestion()});const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();